server:
  port: 8000

spring:
  application:
    name: missav-bot

  sql:
    init:
      mode: never

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3308/missav_bot?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:your_password}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # 生产环境关闭 SQL 日志，开发时可以打开
    # log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0

telegram:
  bot:
    token: ${BOT_TOKEN:YOUR_BOT_TOKEN}
    username: ${BOT_USERNAME:YOUR_BOT_USERNAME}
    default-chat-id: ${BOT_CHAT_ID:0}

crawler:
  enabled: true
  interval: 900000
  initial-pages: 2
  user-agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

logging:
  level:
    root: INFO
    com.missav.bot: INFO
    com.missav.bot.crawler: INFO
    com.missav.bot.push: INFO
    com.missav.bot.scheduler: INFO
    org.telegram: WARN
    com.zaxxer.hikari: WARN
    org.springframework: WARN
    # 如需调试 SQL，取消下面的注释
    # com.missav.bot.**.mapper: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/missav-bot.log

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: when-authorized
