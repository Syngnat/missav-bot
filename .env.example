# 数据库密码
DB_PASSWORD=your_secure_password

# Telegram Bot Token (从 @BotFather 获取)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram Bot 用户名
BOT_USERNAME=YourBotUsername

# Telegram 群组/频道 Chat ID (可选，用于默认推送)
# 获取方法：将 bot 加入群组后，发送一条消息，然后访问：
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# 在返回的 JSON 中找到 "chat":{"id": -100xxxxxxxxx}
BOT_CHAT_ID=0

# ============ 日志级别配置（可选）============
# 日志级别: TRACE, DEBUG, INFO, WARN, ERROR
# 默认值都是 INFO，调试时可以改为 DEBUG

# 应用日志级别（默认 INFO）
# LOG_LEVEL_APP=DEBUG

# SQL 日志级别（默认 INFO，改为 DEBUG 可查看所有 SQL）
# LOG_LEVEL_SQL=DEBUG

# 爬虫日志级别（默认 INFO）
# LOG_LEVEL_CRAWLER=DEBUG

# 推送日志级别（默认 INFO）
# LOG_LEVEL_PUSH=DEBUG

# 定时任务日志级别（默认 INFO）
# LOG_LEVEL_SCHEDULER=DEBUG
