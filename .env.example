# 数据库密码
DB_PASSWORD=your_secure_password

# Telegram Bot Token (从 @BotFather 获取)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram Bot 用户名
BOT_USERNAME=YourBotUsername

# Telegram 群组/频道 Chat ID (可选，用于默认推送)
# 获取方法：将 bot 加入群组后，发送一条消息，然后访问：
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# 在返回的 JSON 中找到 "chat":{"id": -100xxxxxxxxx}
BOT_CHAT_ID=0

# ============ 代理配置（可选）============
# 如果你的服务器无法直接访问 Telegram API，可以启用代理
# 注意：生产环境通常不需要代理，只在开发环境或特殊网络环境下使用

# 是否启用代理（默认 false）
# TELEGRAM_PROXY_ENABLED=true

# 代理服务器地址（默认 127.0.0.1）
# TELEGRAM_PROXY_HOST=127.0.0.1

# 代理服务器端口（默认 7890）
# TELEGRAM_PROXY_PORT=7890

# ============ 爬虫配置（可选）============

# 是否启用爬虫（默认 true）
# CRAWLER_ENABLED=true

# 爬取间隔时间（毫秒，默认 900000 = 15分钟）
# CRAWLER_INTERVAL=900000

# 首次启动爬取页数（默认 2）
# CRAWLER_INITIAL_PAGES=2

# 清理任务执行时间（cron 表达式，默认每天凌晨3点）
# 格式：秒 分 时 日 月 星期
# 示例：
#   - 每天凌晨3点: 0 0 3 * * ?
#   - 每6小时: 0 0 */6 * * ?
#   - 每周日凌晨2点: 0 0 2 * * 0
# CRAWLER_CLEANUP_CRON=0 0 3 * * ?

# ============ 日志级别配置（可选）============
# 日志级别: TRACE, DEBUG, INFO, WARN, ERROR
# 默认值都是 INFO，调试时可以改为 DEBUG

# 应用日志级别（默认 INFO）
# LOG_LEVEL_APP=DEBUG

# SQL 日志级别（默认 INFO，改为 DEBUG 可查看所有 SQL）
# LOG_LEVEL_SQL=DEBUG

# 爬虫日志级别（默认 INFO）
# LOG_LEVEL_CRAWLER=DEBUG

# 推送日志级别（默认 INFO）
# LOG_LEVEL_PUSH=DEBUG

# 定时任务日志级别（默认 INFO）
# LOG_LEVEL_SCHEDULER=DEBUG
